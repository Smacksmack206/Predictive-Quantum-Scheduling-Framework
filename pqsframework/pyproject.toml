# This project was generated with 0.3.25 using template: https://github.com/beeware/briefcase-template @ v0.3.25
[tool.briefcase]
project_name = "PQS Framework 40-Qubit"
bundle = "com.hmMediaLabs.pqsframework"
version = "0.0.1"
url = "https://pqsframework.hmMediaLabs.com/pqsframework"
license.file = "LICENSE"
author = "Cedric Vallieu"
author_email = "cvallieu94@gmail.com"

[tool.briefcase.app.pqsframework]
formal_name = "PQS Framework 40-Qubit"
description = "The world's first Predictive-Quantum Scheduling (PQS) Framework - a revolutionary quantum-enhanced energy management"
long_description = """More details about the app should go here.
"""
icon = "pqs-icon"
sources = [
    "src/pqsframework",
]
test_sources = [
    "tests",
]

requires = [
    "rumps",
    "psutil", 
    "flask",
    "numpy",
    "Jinja2",
    "Werkzeug",
    "click",
    "MarkupSafe",
    "itsdangerous",
    "blinker"
]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.pqsframework.macOS]
universal_build = true
system_requires = "macOS 15.0"
deployment_target = "15.0"
requires = [
    "rumps",
    "psutil", 
    "flask",
    "numpy",
    "Jinja2",
    "Werkzeug",
    "click",
    "MarkupSafe",
    "itsdangerous",
    "blinker"
]

[tool.briefcase.app.pqsframework.macOS.info]
LSMinimumSystemVersion = "15.0.0"
LSArchitecturePriority = ["x86_64", "arm64"]
LSRequiresNativeExecution = false
NSHighResolutionCapable = true
LSUIElement = true
NSSystemAdministrationUsageDescription = "PQS Framework needs system access for quantum energy optimization on Intel and Apple Silicon Macs."
NSAppleEventsUsageDescription = "PQS Framework uses Apple Events for system integration across all Mac architectures."

[tool.briefcase.app.pqsframework.macOS.info.NSAppTransportSecurity]
NSAllowsArbitraryLoads = true

[tool.briefcase.app.pqsframework.macOS.entitlements]
"com.apple.security.automation.apple-events" = true
"com.apple.security.network.client" = true
"com.apple.security.network.server" = true
"com.apple.security.files.user-selected.read-write" = true

[tool.briefcase.app.pqsframework.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.pqsframework.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.pqsframework.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.pqsframework.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.pqsframework.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.pqsframework.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "24.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.pqsframework.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.pqsframework.iOS]
requires = [
    # Add your iOS-specific app requirements here
]

[tool.briefcase.app.pqsframework.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.pqsframework.web]
requires = [
    # Add your web-specific app requirements here
]

